# components/heltec_lora/component.yaml
name: heltec_lora
platforms: [esp32]       # OK to keep; limits build to ESP32 targets
required: true           # ← THIS forces the sources to be built
version: "1.0.0"

build:
  src: [heltec_lora.cpp]
  include: [heltec_lora.h]

dependencies:
  - Heltec_ESP32         # if you really need the Arduino Heltec lib
  - esphome
