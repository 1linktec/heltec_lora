# components/heltec_lora/component.yaml
name: heltec_lora
platforms: [esp32]
version: "1.0.2"

build:
  src:     [heltec_lora.cpp]
  include: [heltec_lora.h]

# ↓ ONE string, the GitHub HTTPS URL
libraries:
  - https://github.com/Heltec-Aaron-Lee/Heltec_ESP32

dependencies:
  - esphome
