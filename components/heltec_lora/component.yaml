name: heltec_lora
platforms: [esp32]
version: "0.1.0"

# C++ build instructions
build:
  src: [ heltec_lora.cpp ]
  include: [ heltec_lora.h ]

# Pull the upstream Arduino library that actually drives the SX1262
libraries:
  - HeltecAutomation/Heltec ESP32          # same slug you’d use in platformio
