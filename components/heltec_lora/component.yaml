name: heltec_lora
version: "1.0.0"

build:
  src:
    - heltec_lora.cpp        # C++ file(s) to compile
  include:
    - heltec_lora.h          # header(s) to expose

dependencies:
  - Heltec_ESP32             # LoRa driver
  - esphome                  # gives <esphome/...> headers
