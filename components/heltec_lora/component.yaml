# components/heltec_lora/component.yaml
name: heltec_lora
platforms: [esp32]           # ← add this line
version: "1.0.0"

build:
  src: [heltec_lora.cpp]
  include: [heltec_lora.h]

dependencies:
  - Heltec_ESP32
  - esphome
