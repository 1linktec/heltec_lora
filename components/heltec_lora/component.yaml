# components/heltec_lora/component.yaml
name: heltec_lora
platforms: [esp32]
version: "1.0.1"

build:
  src:    [heltec_lora.cpp]
  include: [heltec_lora.h]

# Pull the official Heltec library directly from GitHub
libraries:
  - github: Heltec-Aaron-Lee/Heltec_ESP32   # contains heltec.h

dependencies:
  - esphome
